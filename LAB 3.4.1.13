# step 1 - create an empty list named beatles;
beatles = []

# step 2 - use the append() method to add the following members of the band to the list: John Lennon, Paul McCartney, and George Harrison;
beatles.append("John Lennon")
beatles.append("Paul McCartney")
beatles.append("George Harrison")
print("\nStep 2 - put beatles in list:\n", beatles, "\n")

# step 3 - use the for loop and the append() method to prompt the user to add the following members of the band to the list: Stu Sutcliffe, and Pete Best;
for i in range(2):      # 'i' is an integer setup to the range to run n times; where n is set to 2 (can be more, or less).
    beatles.append(input("New band member: "))
print("Step 3 - now we have more beatles in list:\n", beatles)

# step 4 - use the del instruction to remove Stu Sutcliffe and Pete Best from the list;
del beatles[-1]         # FYI -2 removes the 2nd from last person NOT two people
del beatles[-1]
print("\nStep 4 - removed beatles from list:\n", beatles)

# step 5 - use the insert() method to add Ringo Starr to the beginning of the list.
for i in range(1):
    beatles.insert(0, "Ringo Starr")
print("\nStep 5 - Ringo Starr is now in the list:\n", beatles)

# testing list legth
print("\nThe Fab", len(beatles), "\n")

##########################################################
